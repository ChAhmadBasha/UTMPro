@{ ViewData["Title"] = "Security"; }
<h1 class="text-2xl font-bold mb-6">Security</h1>
<div class="bg-white rounded-xl border p-6 max-w-2xl">
    <h2 class="font-semibold mb-4">Workspace Security</h2>
    <p class="text-gray-500 text-sm mb-4">Enterprise security features (SAML SSO, SCIM) available in Phase 2.</p>
    <div class="space-y-3 text-sm">
        <div class="flex justify-between items-center p-3 bg-gray-50 rounded-lg"><span>Two-factor authentication</span><span class="text-gray-400">Coming soon</span></div>
        <div class="flex justify-between items-center p-3 bg-gray-50 rounded-lg"><span>SAML SSO</span><span class="text-gray-400">Enterprise</span></div>
        <div class="flex justify-between items-center p-3 bg-gray-50 rounded-lg"><span>SCIM directory sync</span><span class="text-gray-400">Enterprise</span></div>
    </div>
</div>
